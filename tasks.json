{
  "tasks": {
    "bootstrap": [
      "lerna bootstrap"
    ],
    "build": [
      "[bootstrap]",
      "[lint]",
      "[compile]"
    ],
    "clean": [
      "rimraf packages/*/bin",
      "rimraf packages/*/lib",
      "rimraf **/*.tsbuildinfo"
    ],
    "compile": [
      "lerna run build"
    ],
    "lint": [
      "jsonsort *.json",
      "jsonsort executables/*/*.json",
      "jsonsort packages/*/*.json"
    ],
    "test": [
      "[build]",
      "nyc mocha --opts mocha.opts"
    ],
    "test:ci": [
      "npm install",
      "[bootstrap]",
      "[compile]",
      "nyc mocha --opts mocha.opts --reporter spec"
    ],
    "watch": [
      "lerna run --parallel watch"
    ],
    "canary": [
      "[test:ci]",
      "lerna publish --conventional-commits --conventional-graduate --canary --yes"
    ],
    "prerelease": [
      "[test:ci]",
      "lerna publish --conventional-commits --conventional-prerelease --yes"
    ],
    "release": [
      "[test:ci]",
      "lerna publish --conventional-commits --yes"
    ]
  }
}
